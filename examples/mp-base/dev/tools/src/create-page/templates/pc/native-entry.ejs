import App from '@pc/modules/<%= pageName %>';
import { store } from '@pc/modules/<%= pageName %>/model/store';
import generatePage from '@pc-native/helpers/generate-page';

generatePage(<App />, {
  store,
});

export default {
  title: 'pc-Native',
  template: 'disable-rem',
  langJsonPicker: ['<%= pageName %>', 'global'],
};
