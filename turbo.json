{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "dev": {
      "outputs": ["dist/**"],
      "cache": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    }
  }
}
